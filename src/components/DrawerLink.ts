import React from 'react'

type DrawerLinkProps = {
    name: string,
    active: boolean,
    internal: boolean
}

export const DrawerLink = ({name, active, internal}: DrawerLinkProps) => {
    return ;
}